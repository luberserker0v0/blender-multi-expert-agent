# README Index

This project has several README files. Use this index to avoid opening stale or
scope-specific notes when you need the current source of truth.

## Repository Entry Points

- [`../README.md`](../README.md)
  Main project quick start. Covers the AO-backed multi-expert runtime, local
  setup, CLI usage, React UI startup, and local CI/CD commands.
- [`index.md`](index.md)
  Documentation map for architecture, runtime, UI, Blender/MCP, testing, and
  follow-up work.

## UI

- [`../ui/README.md`](../ui/README.md)
  React + TypeScript + Vite workspace. Covers UI scripts, AO/MCP readiness
  assumptions, mock mode, and Playwright MCP pointers.
- [`../ui/docs/playwright-mcp.md`](../ui/docs/playwright-mcp.md)
  Browser automation notes for the UI mock/dev workflow.

## Local-Only Assets

- [`../models/yolo/README.md`](../models/yolo/README.md)
  Local YOLO model weight placement. The `models/` directory is ignored by git,
  so this README may exist only in local workspaces unless the model directory
  is deliberately added later.

## Generated / Ignored READMEs

- `.pytest_cache/README.md`
  Generated by pytest and ignored by git. Do not edit or reference it in
  project documentation.

## Maintenance Rules

- Keep root `README.md` focused on quick start and current commands.
- Keep detailed architecture in `docs/`.
- Keep UI-specific workflow in `ui/README.md`.
- Do not document removed runtime entry points as active commands.
- If a README references Agent Orchestrator readiness or settings, use AO
  terminology rather than removed local-model terminology.
